import { Response, Request } from "express";
const signIn = (res: Response, req: Request) => {
  console.log("signIn");
};
const signUp = (res: Response, req: Request) => {
  console.log("signUp");
};
